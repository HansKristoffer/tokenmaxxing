# Server config. All optional; real env vars win over this file (Bun loads .env natively).
# PORT=8787
# Where the SQLite DB lives. Mount the Railway volume here. Default: /data in production, ./.data in dev.
# TOKENMAXXING_DATA_DIR=/data
# NODE_ENV=production
